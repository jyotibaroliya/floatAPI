info:
  name: GET
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
