info:
  name: roles dropdown
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/resource-roles/dropdown"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
