info:
  name: add role to project
  type: http
  seq: 20

http:
  method: POST
  url: "{{base_url}}/api/projects/22/team/roles"
  body:
    type: json
    data: "{\r

      \  \"resourceRoleId\": 3,\r

      \  \"phaseId\": null\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
