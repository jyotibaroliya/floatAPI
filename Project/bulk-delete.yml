info:
  name: bulk-delete
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/api/projects/bulk-delete"
  body:
    type: json
    data: "{\r

      \  \"ids\": [\r

      \    2,3\r

      \  ]\r

      }"
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
