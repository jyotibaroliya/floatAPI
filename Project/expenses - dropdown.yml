info:
  name: expenses - dropdown
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/api/projects/expenses/dropdown"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
