info:
  name: update project stage
  type: http
  seq: 24

http:
  method: PUT
  url: "{{base_url}}/api/projects/880/stage"
  body:
    type: json
    data: |-
      {
        "stageId": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
