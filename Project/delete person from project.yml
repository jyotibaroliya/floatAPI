info:
  name: delete person from project
  type: http
  seq: 22

http:
  method: DELETE
  url: "{{base_url}}/api/projects/22/team/15"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
