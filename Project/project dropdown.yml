info:
  name: project dropdown
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/projects/dropdown"
  body:
    type: text
    data: "{\r

      \  \"email\": \"testdhwani@yopmail.com\",\r

      \  \"password\": \"TestDhwani@123\"\r

      }"
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
