info:
  name: bulk-active
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/api/projects/bulk-activate"
  body:
    type: json
    data: "{\r

      \  \"ids\": [\r

      \    4\r

      \  ]\r

      }"
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
