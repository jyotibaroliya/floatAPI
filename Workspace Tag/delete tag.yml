info:
  name: delete tag
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/api/workspace/tags/1"
  params:
    - name: tagtype
      value: project
      type: query
      disabled: true
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
