info:
  name: fetch timeoff group types
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/time-off/groups/1/types"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
