info:
  name: Change Status
  type: http
  seq: 8

http:
  method: PUT
  url: "{{base_url}}/api/workspace/46/status"
  body:
    type: json
    data: "{\r\n  \"isActive\": true\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
