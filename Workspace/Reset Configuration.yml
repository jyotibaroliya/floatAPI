info:
  name: Reset Configuration
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/api/workspace/{{workspace_id}}/modules/{{module_id}}/fields/reset"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
