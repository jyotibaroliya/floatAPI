info:
  name: ModuleList
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/workspace/{{workspace_id}}/modules"
  params:
    - name: ""
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
