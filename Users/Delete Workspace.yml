info:
  name: Delete Workspace
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{base_url}}/api/Users/35"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
