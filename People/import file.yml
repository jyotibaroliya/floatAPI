info:
  name: import file
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/api/people/import?File"
  params:
    - name: File
      value: ""
      type: query
  body:
    type: multipart-form
    data:
      - name: File
        type: file
        value:
          - /C:/Users/jyoti.baroliya/Desktop/people-import-template.csv
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
