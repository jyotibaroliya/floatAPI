info:
  name: bulk-archive
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/people/bulk-archive"
  body:
    type: json
    data: "{\r

      \  \"ids\": [\r

      \    4,9\r

      \  ]\r

      }"
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
