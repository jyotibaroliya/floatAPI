info:
  name: fetch options
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/allocations/options"
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
