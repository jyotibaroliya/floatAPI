info:
  name: delete team holiday
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/api/time-off/team-holidays/1"
  params:
    - name: year
      value: "2026"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
