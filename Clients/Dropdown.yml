info:
  name: Dropdown
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/clients/dropdown"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
