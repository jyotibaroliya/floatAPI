info:
  name: List
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/clients/list"
  body:
    type: json
    data: "    {\r

      \    \"search\": \"\",\r

      \    \"page\": 1,\r

      \    \"pageSize\": 10\r

      \    }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
