info:
  name: Create
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/clients"
  body:
    type: json
    data: "{\r

      \  \"clientName\": \"nikhil5\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
