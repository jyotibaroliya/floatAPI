info:
  name: get timeoff settings
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/time-off/settings/approvals"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
