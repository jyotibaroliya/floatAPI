info:
  name: Delete
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/api/timezones/14"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
