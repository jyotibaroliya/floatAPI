info:
  name: Dropdown
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/timezones/dropdown"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
