info:
  name: GET By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/timezones/14"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
