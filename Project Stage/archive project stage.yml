info:
  name: archive project stage
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/api/projects/stages/archive"
  body:
    type: json
    data: "{\r

      \  \"stageIds\": [\r

      \    9,10\r

      \  ]\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
