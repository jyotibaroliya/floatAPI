info:
  name: get public holidays
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/time-off/public-holidays"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
