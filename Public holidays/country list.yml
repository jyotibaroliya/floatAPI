info:
  name: country list
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/time-off/public-holidays/countries"
  body:
    type: text
    data: "{\r

      \"groupName\":\"TestUpdate\"\r

      }"
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
