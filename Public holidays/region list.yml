info:
  name: region list
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/time-off/public-holidays/regions?country=US"
  params:
    - name: country
      value: US
      type: query
  body:
    type: text
    data: "{\r

      \"groupName\":\"TestUpdate\"\r

      }"
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
