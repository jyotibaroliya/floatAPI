info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/api/project-stages/5"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
