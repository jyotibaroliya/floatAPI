info:
  name: Get By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/project-stages/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
