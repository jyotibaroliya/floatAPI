info:
  name: Refresh token
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/api/Auth/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
