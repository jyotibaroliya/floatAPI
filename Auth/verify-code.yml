info:
  name: verify-code
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/Auth/verify-code"
  body:
    type: json
    data: "{\r

      \  \"userId\": \"61\",\r

      \  \"code\": \"501419\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
