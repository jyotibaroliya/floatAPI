info:
  name: me
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/Auth/me"
  body:
    type: text
    data: "{\r

      \  \"email\": \"testdhwani@yopmail.com\",\r

      \  \"password\": \"TestDhwani@123\"\r

      }"
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
