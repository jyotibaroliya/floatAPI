info:
  name: me
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/Auth/me"
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

runtime:
  variables:
    - name: base_url
      value: http://10.37.58.123:8087

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
