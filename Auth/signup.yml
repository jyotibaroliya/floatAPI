info:
  name: signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/Auth/signup"
  body:
    type: json
    data: |-
      {
        "name": "Test ProjectStage ",
        "email": "testprojectstage@yopmail.com",
        "password": "TestProjectStage@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
