info:
  name: get userId permission
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/access-control/users/1021/permissions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
