info:
  name: get roles
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/access-control/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
