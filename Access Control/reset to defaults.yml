info:
  name: reset to defaults
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/api/access-control/role-permissions/reset-to-defaults"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
