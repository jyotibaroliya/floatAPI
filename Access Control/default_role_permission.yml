info:
  name: default_role_permission
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/access-control/default-role-permissions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
