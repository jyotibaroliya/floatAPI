info:
  name: role_permissions
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/access-control/role-permissions"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
