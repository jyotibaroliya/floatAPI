info:
  name: RUN
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/migrations/tenant/run"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
